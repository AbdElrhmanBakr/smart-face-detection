import "./Intro.css";

const Intro = () => {
  return (
    <section>
      <div className="intro-container">
        <span className="type">
          This is a CSS only solution for a multi-line typewriter effect.
        </span>
      </div>
    </section>
  );
};

export default Intro;
